class DeleteMe {
  constructor() {
    this.message = 'This message is from test.js. Delete this folder when implementing share image.';
  }
}

const deleteMe = new DeleteMe();
console.log(`message: ${deleteMe.message}`);
